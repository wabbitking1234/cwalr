# Cwalr
That's all it does, adds the walking bomb to your menu and makes them spin.    

Sadly the spinning relies on bytecode manipulation to replace the existing draw funcgtion, so it's not compatible with mobile. But the main menu units do still spawn and function as normal.